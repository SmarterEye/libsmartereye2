target_sources(${LSE2_TARGET}
        PRIVATE
        "${CMAKE_CURRENT_LIST_DIR}/sensor.cc"
        "${CMAKE_CURRENT_LIST_DIR}/notification.cc"

        "${CMAKE_CURRENT_LIST_DIR}/sensor.h"
        "${CMAKE_CURRENT_LIST_DIR}/notification.h"
        )